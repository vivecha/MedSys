<UserControl x:Class="MedicalSystem.Views.DoctorsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header and Search -->
        <StackPanel Grid.Row="0" Margin="20,20,20,10">
            <TextBlock Text="Управление врачами" Style="{StaticResource HeaderText}"/>

            <Border Background="White" CornerRadius="5" Padding="10" BorderBrush="#FFDDDDDD" BorderThickness="1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Search Box -->
                    <StackPanel Grid.Column="0" Orientation="Horizontal">
                        <TextBox x:Name="SearchTextBox" Width="300" Style="{StaticResource FormTextBox}" 
                         Margin="0,0,10,0" Padding="5"/>
                        <Button Content="Поиск" Style="{StaticResource ActionButton}" Margin="10,0,0,0" Click="SearchDoctors_Click"/>
                    </StackPanel>

                    <!-- Action Buttons -->
                    <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="Добавить врача" Style="{StaticResource ActionButton}" 
                        Margin="10,0,0,0" Click="AddDoctor_Click"/>
                        <Button Content="Обновить" Style="{StaticResource SecondaryButton}" 
                        Margin="10,0,0,0" Click="RefreshDoctors_Click"/>
                    </StackPanel>
                </Grid>
            </Border>
        </StackPanel>

        <!-- Data Grid -->
        <Border Grid.Row="1" Margin="20,0,20,20" Background="White" CornerRadius="5" 
                BorderBrush="#FFDDDDDD" BorderThickness="1">
            <DataGrid x:Name="DoctorsGrid" Style="{StaticResource DataGridStyle}" 
                      ColumnHeaderStyle="{StaticResource DataGridColumnHeaderStyle}"
                      AutoGenerateColumns="False" SelectionMode="Single">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding DoctorId}" Width="Auto"/>
                    <DataGridTextColumn Header="Фамилия" Binding="{Binding LastName}" Width="*"/>
                    <DataGridTextColumn Header="Имя" Binding="{Binding FirstName}" Width="*"/>
                    <DataGridTextColumn Header="Отчество" Binding="{Binding MiddleName}" Width="*"/>
                    <DataGridTextColumn Header="Специальность" Binding="{Binding SpecialtyName}" Width="*"/>
                    <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>