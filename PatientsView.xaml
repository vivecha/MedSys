<UserControl x:Class="MedicalSystem.Views.PatientsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             xmlns:local="clr-namespace:MedicalSystem"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1000">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header and Search -->
        <StackPanel Grid.Row="0" Margin="20,20,20,10">
            <TextBlock Text="Управление пациентами" Style="{StaticResource HeaderText}"/>

            <Border Background="White" CornerRadius="5" Padding="10" BorderBrush="#FFDDDDDD" BorderThickness="1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Search Box -->
                    <StackPanel Grid.Column="0" Orientation="Horizontal">
                        <TextBox x:Name="SearchTextBox" Width="300" Style="{StaticResource FormTextBox}" 
                                 Margin="0,0,10,0" Padding="5"/>
                        <Button Content="Поиск" Style="{StaticResource ActionButton}" Margin="10,0,0,0" Click="SearchPatients_Click"/>
                    </StackPanel>

                    <!-- Action Buttons -->
                    <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="Добавить пациента" Style="{StaticResource ActionButton}" 
                                Margin="10,0,0,0" Click="AddPatient_Click"/>
                        <Button Content="Обновить" Style="{StaticResource SecondaryButton}" 
                                Margin="10,0,0,0" Click="RefreshPatients_Click"/>
                    </StackPanel>
                </Grid>
            </Border>
        </StackPanel>

        <!-- Data Grid -->
        <Border Grid.Row="1" Margin="20,0,20,20" Background="White" CornerRadius="5" 
                BorderBrush="#FFDDDDDD" BorderThickness="1">
            <DataGrid x:Name="PatientsGrid" Style="{StaticResource DataGridStyle}" 
                      ColumnHeaderStyle="{StaticResource DataGridColumnHeaderStyle}"
                      AutoGenerateColumns="False" SelectionMode="Single">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding PatientId}" Width="Auto"/>
                    <DataGridTextColumn Header="Фамилия" Binding="{Binding LastName}" Width="*"/>
                    <DataGridTextColumn Header="Имя" Binding="{Binding FirstName}" Width="*"/>
                    <DataGridTextColumn Header="Отчество" Binding="{Binding MiddleName}" Width="*"/>
                    <DataGridTextColumn Header="Дата рождения" Binding="{Binding DateOfBirth, StringFormat=d}" Width="Auto"/>
                    <DataGridTextColumn Header="Пол" Binding="{Binding Gender}" Width="Auto"/>
                    <DataGridTextColumn Header="Телефон" Binding="{Binding PhoneNumber}" Width="Auto"/>
                    <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="Auto"/>
                    <DataGridTextColumn Header="Адрес" Binding="{Binding Address}" Width="Auto"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>